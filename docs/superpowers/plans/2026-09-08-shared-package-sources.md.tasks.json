{
  "planPath": "docs/superpowers/plans/2026-09-08-shared-package-sources.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Discovery skips _shared/, follows symlinks",
      "status": "pending",
      "description": "**Goal:** `find_packages`/`find_projects` never treat `_shared/` as a project or package, while a symlinked package directory is discovered under its lexical subproject with that subproject's macros.\n\n**Files:**\n- Modify: `percona_obs/common.py:117-145` (`is_package`, `is_project`, `find_packages`) and `percona_obs/common.py:590-602` (`find_projects`)\n- Test: `tests/test_shared_source.py` (create)\n\n**Acceptance Criteria:**\n- [ ] `SHARED_SOURCE_DIRNAME == \"_shared\"` and `is_shared_source_dir(path)` exist in `percona_obs/common.py`.\n- [ ] `find_packages(root/ppg/staging, \"X:ppg:staging\")` yields no entry whose project contains `:_shared` and no path under `_shared/`.\n- [ ] `find_projects(root/ppg/staging, \"X:ppg:staging\")` yields no `:_shared` project.\n- [ ] A symlinked package `staging/17/pkg -> ../_shared/pkg` is yielded as `(\"X:ppg:staging:17\", root/ppg/staging/17/pkg)` and `load_macros(that_path)[\"PG_MAJOR_VERSION\"] == \"17\"`.\n\n**Verify:** `venv/bin/python -m pytest tests/test_shared_source.py -q -k discovery` → all pass; `venv/bin/black percona_obs/ && venv/bin/pyright` → `0 errors`.\n\nFull steps and code: plan Task 1.\n\n```json:metadata\n{\"files\": [\"percona_obs/common.py\", \"tests/test_shared_source.py\"], \"verifyCommand\": \"venv/bin/python -m pytest tests/test_shared_source.py -q -k discovery && venv/bin/black percona_obs/ && venv/bin/pyright\", \"acceptanceCriteria\": [\"SHARED_SOURCE_DIRNAME and is_shared_source_dir exist\", \"find_packages skips _shared and yields symlinked packages under their subproject\", \"find_projects skips _shared\", \"load_macros on symlink path resolves the subproject's PG_MAJOR_VERSION\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: git_utils sees through symlinked package dirs",
      "status": "pending",
      "blockedBy": [1],
      "description": "**Goal:** Every git-scoped check in `git_utils` includes the symlink target's path, so an edit under `_shared/` counts as a change (committed or dirty) for each linking package.\n\n**Files:**\n- Modify: `percona_obs/git_utils.py:1-6` (imports), `:54-137` (the three `_has_*_since` functions), `:142-162` (`_is_path_dirty`)\n- Test: `tests/test_shared_source.py` (extend)\n\n**Acceptance Criteria:**\n- [ ] `git_utils._package_pathspecs(Path)` returns `[path]` for a real dir and `[path, lexical_target]` for a symlinked dir; the target is computed with `os.readlink` + `os.path.normpath`, not `Path.resolve()`.\n- [ ] After committing an edit to `_shared/pkg/debian/pgversions`, `_has_package_changes_since`, `_has_package_content_changes_since` and `_has_non_obs_package_changes_since` all return True for the 17 link.\n- [ ] After committing an edit to `_shared/pkg/obs/_service` only, `_has_non_obs_package_changes_since` returns False.\n- [ ] With an uncommitted edit to `_shared/pkg/rpm/pkg.spec`, `_is_path_dirty(staging/17/pkg)` returns True.\n- [ ] With no edits, all four return False.\n- [ ] `_macros_changed_since` stays per-subproject (17 bump → True for 17 link, False for 14 link).\n\n**Verify:** `venv/bin/python -m pytest tests/test_shared_source.py -q` → all pass; `venv/bin/black percona_obs/ && venv/bin/pyright` → `0 errors`; `venv/bin/python -m pytest -q` → all pass.\n\nFull steps and code: plan Task 2.\n\n```json:metadata\n{\"files\": [\"percona_obs/git_utils.py\", \"tests/test_shared_source.py\"], \"verifyCommand\": \"venv/bin/python -m pytest tests/test_shared_source.py -q && venv/bin/black percona_obs/ && venv/bin/pyright && venv/bin/python -m pytest -q\", \"acceptanceCriteria\": [\"_package_pathspecs returns lexical symlink target\", \"committed shared edits detected via link\", \"obs-only shared edit is obs-only\", \"dirty shared edit detected via link\", \"no-change returns False\", \"_macros_changed_since per subproject\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Move pg_stat_monitor to _shared/, link 14-18",
      "status": "pending",
      "blockedBy": [2],
      "description": "**Goal:** One canonical packaging copy under `root/ppg/staging/_shared/percona-pg_stat_monitor/`, with `root/ppg/staging/{14,15,16,17,18}/percona-pg_stat_monitor` as relative symlinks, rendering the right PG major per subproject.\n\n**Files:**\n- Create (via `git mv`): `root/ppg/staging/_shared/percona-pg_stat_monitor/` (from `root/ppg/staging/18/percona-pg_stat_monitor/`)\n- Create: `root/ppg/staging/_shared/percona-pg_stat_monitor/debian/source/options` (moved from the 17 copy)\n- Delete: `root/ppg/staging/{14,15,16,17}/percona-pg_stat_monitor/`\n- Create: symlinks `root/ppg/staging/{14,15,16,17,18}/percona-pg_stat_monitor -> ../_shared/percona-pg_stat_monitor`\n\n**Acceptance Criteria:**\n- [ ] `git ls-files -s root/ppg/staging/1?/percona-pg_stat_monitor` shows five mode-`120000` entries pointing to `../_shared/percona-pg_stat_monitor`.\n- [ ] `_shared/` equals the old staging/18 copy plus `debian/source/options`.\n- [ ] Render script (plan Task 3 step 3) prints `14 14 14`, `17 17 17`, `18 18 18`.\n- [ ] `venv/bin/python -m pytest -q` passes; `git status` clean after commit.\n\n**Verify:** render script output as above; `git ls-files -s root/ppg/staging/1?/percona-pg_stat_monitor | awk '{print $1}' | sort -u` → `120000`.\n\nFull steps and commands: plan Task 3.\n\n```json:metadata\n{\"files\": [\"root/ppg/staging/_shared/percona-pg_stat_monitor\", \"root/ppg/staging/14/percona-pg_stat_monitor\", \"root/ppg/staging/15/percona-pg_stat_monitor\", \"root/ppg/staging/16/percona-pg_stat_monitor\", \"root/ppg/staging/17/percona-pg_stat_monitor\", \"root/ppg/staging/18/percona-pg_stat_monitor\"], \"verifyCommand\": \"git ls-files -s root/ppg/staging/1?/percona-pg_stat_monitor | awk '{print $1}' | sort -u && venv/bin/python -m pytest -q\", \"acceptanceCriteria\": [\"five 120000 symlink entries\", \"_shared equals old 18 copy plus debian/source/options\", \"render prints correct majors for 14/17/18\", \"tests pass\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Document the _shared/ convention",
      "status": "pending",
      "blockedBy": [3],
      "description": "**Goal:** The tree README, the tool reference and the architecture reference describe `_shared/` and the symlink convention, including change-detection behaviour and the macro-definition constraint.\n\n**Files:**\n- Modify: `root/README.md:126-147`\n- Modify: `docs/PERCONA_OBS_TOOL.md:253-267`\n- Modify: `docs/PACKAGING_HOWTO.md` (new section before line 534)\n- Modify: `.github/copilot-instructions.md:311`\n\n**Acceptance Criteria:**\n- [ ] `root/README.md` staging table has a `_shared/` row and the layout block shows a symlinked package example.\n- [ ] Each doc states: `_shared/` skipped by discovery; symlinks relative; macros render from the symlink location; referenced macros must be defined for every linking major; git checks include the target.\n- [ ] `grep -n '_shared' root/README.md docs/PERCONA_OBS_TOOL.md docs/PACKAGING_HOWTO.md .github/copilot-instructions.md` hits all 4 files.\n\n**Verify:** the grep above → 4 files matched.\n\nFull text: plan Task 4.\n\n```json:metadata\n{\"files\": [\"root/README.md\", \"docs/PERCONA_OBS_TOOL.md\", \"docs/PACKAGING_HOWTO.md\", \".github/copilot-instructions.md\"], \"verifyCommand\": \"grep -l '_shared' root/README.md docs/PERCONA_OBS_TOOL.md docs/PACKAGING_HOWTO.md .github/copilot-instructions.md | wc -l\", \"acceptanceCriteria\": [\"README table row + tree example\", \"all five rules stated\", \"grep hits 4 files\"], \"modelTier\": \"mechanical\"}\n```"
    }
  ],
  "lastUpdated": "2026-09-08T00:00:00Z"
}
