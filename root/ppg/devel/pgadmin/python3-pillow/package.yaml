title: pillow 12.3.0 for Python 3.12
description: |
  Python Imaging Library (Fork). RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Requires setuptools>=77 and pybind11.
  Bump by editing obs/_service (sdist URL) and rpm/*.spec (Version + changelog).
