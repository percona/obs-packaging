title: poetry-core 2.2.1 for Python 3.12
description: |
  Poetry PEP 517 Build Backend. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
