title: Flask-Login 0.6.3 for Python 3.12
description: |
  User authentication and session management for Flask. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
