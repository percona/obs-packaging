title: passlib 1.7.4 for Python 3.12
description: |
  comprehensive password hashing framework supporting over 30 schemes. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
