title: typing-extensions 4.16.0 for Python 3.12
description: |
  Backported and Experimental Type Hints for Python 3.9+. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
