title: h11 0.16.0 for Python 3.12
description: |
  A pure-Python, bring-your-own-I/O implementation of HTTP/1.1. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
