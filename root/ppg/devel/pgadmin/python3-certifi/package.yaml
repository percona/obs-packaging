title: certifi 2026.6.17 for Python 3.12
description: |
  Python package for providing Mozilla's CA Bundle. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
