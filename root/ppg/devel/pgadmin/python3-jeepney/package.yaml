title: jeepney 0.9.0 for Python 3.12
description: |
  Low-level, pure Python DBus protocol wrapper. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
