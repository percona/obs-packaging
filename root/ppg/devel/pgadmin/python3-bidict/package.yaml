title: bidict 0.23.1 for Python 3.12
description: |
  The bidirectional mapping library for Python. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
