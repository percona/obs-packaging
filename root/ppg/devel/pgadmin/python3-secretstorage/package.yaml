title: SecretStorage 3.5.0 for Python 3.12
description: |
  Python bindings to FreeDesktop.org Secret Service API. RPM-only python3.12-* build from the PyPI sdist,
  consumed by pgAdmin 4 (percona-pgadmin4). Bump by editing obs/_service (sdist URL) and
  rpm/*.spec (Version + changelog).
